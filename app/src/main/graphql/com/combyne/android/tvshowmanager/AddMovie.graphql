mutation addMovie($movie: CreateMovieInput!) {
    createMovie(input: $movie) {
        movie {
            title
            releaseDate
            seasons
        }
        clientMutationId
    }
}